name: AI 抓賽程

on:
  schedule:
    - cron: "0 1 * * 1" # 每週一 01:00 UTC = 台北時間 09:00
  workflow_dispatch:
    inputs:
      dry_run:
        description: "試跑（只印結果，不寫入 Sheet）"
        type: boolean
        default: true
      update_dates:
        description: "改期時自動更新既有列的日期（不勾＝只在 log 通知）"
        type: boolean
        default: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 安裝套件
        run: npm ci
        working-directory: scraper

      - name: 純邏輯測試
        run: node test.mjs
        working-directory: scraper

      - name: 執行爬蟲
        run: node index.mjs
        working-directory: scraper
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
          SHEET_TAB: ${{ secrets.SHEET_TAB }}
          DRY_RUN: ${{ (github.event_name == 'workflow_dispatch' && inputs.dry_run) && '1' || '0' }}
          # 排程執行時是否自動更新改期：改這裡的 '0' → '1' 就會開啟
          UPDATE_DATES: ${{ github.event_name == 'workflow_dispatch' && inputs.update_dates && '1' || '0' }}
